You are a story clip segmentation expert.
Split the full text into clip candidates for downstream screenplay conversion.

Full text:
{input}

Location library:
{locations_lib_name}

Character library:
{characters_lib_name}

Character introductions:
{characters_introduction}

Output format (JSON array only):
[
  {
    "start": "exact start snippet from source text (5-15 chars, DO NOT put the entire clip here)",
    "end": "exact end snippet from source text (5-15 chars, DO NOT put the entire clip here)",
    "summary": "short clip summary",
    "location": "best matched location name",
    "characters": ["Character A", "Character B"],
    "props": ["Prop A", "Prop B"]
  }
]

Rules:
⚠️⚠️⚠️ BOUNDARY EXTRACTION RULES - PREVENT SYSTEM CRASH ⚠️⚠️⚠️
- `start` MUST be a short snippet (5-15 chars) copied EXACTLY word-for-word from the VERY BEGINNING of the clip.
- `end` MUST be a short snippet (5-15 chars) copied EXACTLY word-for-word from the VERY END of the clip.
- ❌ NEVER put the entire clip content into the `start` field!
- ❌ NEVER summarize, rephrase, or invent words! Extract the exact text as it appears in the source.
- ❌ NEVER add punctuation (like quotes) if they are not in the source text!
1. Keep clips contiguous, ordered, and fully covering the source text.
2. Prefer natural scene/drama boundaries.
3. Minimize over-splitting.
4. location, characters, and props should prefer exact names from libraries when possible.
5. Return JSON only, no markdown or extra text.
6. ⚠️ JSON SAFETY: All quotation marks in dialogue (""''「」 etc.) MUST be converted to corner brackets「」in JSON string values. NEVER use raw ASCII double quotes " inside string values—they break JSON structure.

Prop library:
{props_lib_name}
